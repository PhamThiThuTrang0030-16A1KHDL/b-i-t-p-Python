# Tham chiếu(reference)
# Tham trị
def tinh_binh_phuong(so):
    # Hàm tính bình phương của 1 số
    so=so*so
    print(" Giá trị của số trong hàm: %d"%so)
    return
so=10
tinh_binh_phuong(so)
print("Giá trị ngoài hàm: %d"%so)
